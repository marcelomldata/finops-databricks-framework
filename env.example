# Exemplo de Configuração - FinOps Databricks Framework
# 
# INSTRUÇÕES:
# 1. Copie este arquivo para .env na raiz do projeto
# 2. Preencha com suas credenciais reais
# 3. NUNCA versionar o arquivo .env (está no .gitignore)
#
# Para versão mais detalhada, veja: config/example.env

# Databricks Configuration
WORKSPACE_NAME=workspace_prod
WORKSPACE_URL=https://adb-xxxxx.azuredatabricks.net
DATABRICKS_TOKEN=dapi_SUBSTITUIR_POR_TOKEN_REAL
DATABRICKS_CLUSTER_ID=

# Azure Configuration (se aplicável)
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
AZURE_SUBSCRIPTION_ID=

# AWS Configuration (se aplicável)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# GCP Configuration (se aplicável)
GCP_PROJECT_ID=
GCP_CREDENTIALS_PATH=/path/to/credentials.json

# Revalidation Configuration
BASELINE_DAYS=30
BILLING_DAYS_BACK=30

# Config Path
CONFIG_PATH=config/config.yaml

# ⚠️ SEGURANÇA: NUNCA commite este arquivo com credenciais reais!
